# Core dependencies
pyyaml>=6.0
requests>=2.31.0
python-dotenv>=1.0.0

# Output formatting
tabulate>=0.9.0
jinja2>=3.1.0

# Optional SSH support (used if available)
paramiko>=3.0.0

# MCP server
mcp>=1.0.0

# Standard library dependencies (usually included with Python)
# json - built-in
# dataclasses - built-in (Python 3.7+)
# typing - built-in
# pathlib - built-in
# datetime - built-in
# telnetlib - built-in
# socket - built-in

